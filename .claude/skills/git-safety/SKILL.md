---
name: git-safety
description: Perform safe Git lifecycle operations with explicit boundaries.
---

# Git Safety
Before mutation inspect branch/status/staged/unstaged diff. Never perform destructive history/state operations without explicit approval. Never push merely because a commit succeeded. Keep commits focused and reviewable.
