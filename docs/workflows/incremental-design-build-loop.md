# Incremental Design / Build Loop

Stage 0 fresh discovery → System Design → Gate A critic → Implementation Design → Gate B critic → Implement → Local validation → Clean validation when warranted → Gate C critic → Git/Human completion.

The author of an artifact should not be its only validator.
