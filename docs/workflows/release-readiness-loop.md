# Release Readiness Loop

Acceptance criteria → traceability → diff/scope review → clean build/tests → security/dependency checks → observability/rollback review → final critic → explicit human release decision.
