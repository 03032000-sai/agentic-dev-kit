# Business Documentation Loop

Scope intake → code map (stable IDs) → domain glossary (gate) → business rule mining (WHAT confirmed / WHY inferred) → capability mapping → critic checkpoint → narrative writing (zero new claims) → critic checkpoint (readability) → publish (traceability matrix, gap register, coverage report).
