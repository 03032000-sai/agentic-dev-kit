# Brownfield Preloop

Before changing an unfamiliar repository: bootstrap instructions/context → discover current behavior → reverse-engineer abstract design → reverse-engineer implementation design → align docs/code → record validation commands → create evidence-backed current-state artifacts → enter requested engineering workflow.
