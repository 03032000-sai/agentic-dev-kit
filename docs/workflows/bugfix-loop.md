# Bugfix Loop

Failure evidence → focused discovery → root-cause hypothesis → design-impact decision → regression test → implementation → local validation → independent critique → clean validation when needed → Git completion.
