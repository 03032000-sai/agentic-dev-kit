---
applyTo: "**/*"
---

For security-sensitive changes preserve least privilege, never expose secrets, do not weaken authorization to make tests pass, identify trust boundaries, prefer deterministic security validation, and require human approval for destructive or credential-sensitive actions.
